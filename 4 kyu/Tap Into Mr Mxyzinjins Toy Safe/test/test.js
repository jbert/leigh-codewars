/* eslint-env mocha */
/* eslint-disable prefer-arrow-callback, func-names */

/*
In process of writing tests, check back in 24 hours


const { expect } = require('chai');
const permutations = require('../permutations.js');

describe('permutations() should return all possible permutations of input string', function () {
  const tests = [
    { args: 'ab', expected: ['ab', 'ba'] },
    { args: 'abc', expected: ['abc', 'acb', 'bac', 'bca', 'cab', 'cba'] },
    { args: 'abcd', expected: ['abcd', 'abdc', 'acbd', 'acdb', 'adbc', 'adcb', 'bacd', 'badc', 'bcad', 'bcda', 'bdac', 'bdca', 'cabd', 'cadb', 'cbad', 'cbda', 'cdab', 'cdba', 'dabc', 'dacb', 'dbac', 'dbca', 'dcab', 'dcba'] },
  ];
  tests.forEach(function (test) {
    it(`Checks all permutations of ${test.args} are ${test.expected}`, function () {
      const res = permutations(test.args);
      expect(res).to.eql(test.expected);
    });
  });
});

*/
